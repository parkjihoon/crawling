# 크롤링 코어
scrapling>=0.4.5

# Scrapling 브라우저 의존성 (StealthyFetcher용)
patchright>=1.58.0
curl_cffi>=0.15.0
browserforge>=1.2.0
msgspec>=0.21.0

# 브라우저 자동화 (DynamicFetcher 폴백용)
playwright>=1.50.0

# YAML 설정 파일
pyyaml>=6.0

# 웹 대시보드
flask>=3.0.0

# 스케줄링 (증분 수집 데몬 모드)
apscheduler>=3.10.0

# LLM 파이프라인 (선택)
# openai>=1.0.0
# anthropic>=0.30.0
